[project]
name = "git-citation"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Joy IZABAYO", email = "joy.izabayondagige@etu.univ-tours.fr" }
]
requires-python = ">=3.14"
dependencies = [
    "pyfiglet>=1.0.4",
    "rich>=15.0.0",
]

[project.scripts]
git-citation = "git_citation:main"

[build-system]
requires = ["uv_build>=0.12.12,<0.13.0"]
build-backend = "uv_build"
